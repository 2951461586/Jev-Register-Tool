# 跑批速率与成功率优化方案

> 依据：2026-09-21 晚 **50 批次全链路复跑**（`--count 50 --concurrency 2`，墙钟 **1552s**，
> `43 keyed / 7 failed`，43 把新 key 真打验收 **43/43** 通过）。
> 数据源：`exports/batch50-20260921.json`、`exports/batch50-20260921.log`。
> 复算命令见 §5。

---

## §1 结论

**三个结论，按杠杆从大到小：**

1. 🔴 **最大浪费是"失败账号的空等"，不是发信速率。**
   6 个超时账号各等 300s，占总墙钟 **58%**。
2. 🔴 **超时阈值 300s 是实测邮件延迟的 66 倍**（实测 max 4.57s）。
   它防的是**上一代链路**的批量审批邮件，前提**已经消失**。
3. **速率与成功率可以同时改善**：降超时 + 超时后重发 + 适度提并发，
   预期 **墙钟 −73%（1552s → ~420s）、成功率 86% → 95%+**。

---

## §2 现状量化

### 2.1 墙钟构成（1552s）

| 构成 | 账 | 占比 |
|---|---|---|
| 43 个成功账号 | 累计 464s（login 276.5 + create_key 187.6）⇒ 并发 2 下约 230s | 15% |
| **6 个超时账号** | 6 × 300s ⇒ 并发 2 下 **900s** | **58%** |
| 其余（建邮箱 / onboarding / 调度） | 约 420s | 27% |

- 单账号：成功 **10.7s**（中位），失败 **300s**
- 当前速率：50 / 1552s = **1.93 个/分钟**

### 2.2 邮件到达延迟（43 个成功账号实测）

口径：`signup.mail_at ÷ 1000 − created_at`（⚠️ 前者毫秒、后者秒，**单位不同**）

```
min 2.66s · P50 3.26s · P90 3.80s · max 4.57s
```

⇒ **正常邮件 3 秒内必到**。6 个失败账号不是"慢"，是**真的丢包**。

### 2.3 失败构成（7 个，全部非我方缺陷）

| 类 | 数 | 原因 |
|---|---|---|
| 站点发信丢包 | 6 | `未在 300s 内收到确认邮件（轮询 132~134 次，5xx 0 次）` |
| 魔法链接已失效 | 1 | `未找到 dfp 交换参数（页面长度 256）` |

### 2.4 🔴 度量黑洞（必须先修，否则下一轮优化仍在猜）

`_fail()` 只写 `stages` / `error` / `status`，**不写 `timings`**；
`timings["login"]` 只在**成功路径末尾**赋值（`src/stages.py:349`）。
⇒ **失败账号的 `timings` 完全是空的**，300s 空等没有任何留痕，
本次只能靠两个时间戳字段**反推**才知道时间花在哪。

---

## §3 优化项

### 优化 A：`MAIL_TIMEOUT` 300s → 60s ＋ 超时后重发（**核心**）

| | 现状 | 改后 |
|---|---|---|
| 超时 | 300s | **60s**（对实测 max 4.57s 仍有 **13 倍**余量） |
| 超时后 | 直接判 `failed`，等下一轮 `relogin_pending` | **立即重发一次**（新邮件），再等 60s |
| 失败账号耗时 | 300s | 60s（丢包）或 ~5s（重发成功） |

**依据**：邮件延迟 P90 3.80s ⇒ 重发大概率 5s 内到达。

⚠️ **两者必须成对做**：只降超时、不加重发，会把偶发慢邮件直接判死，成功率**反而下降**。

**预期**：失败账号等待 900s → 180s（并发 2）⇒ 墙钟 **1552s → ~832s（−46%）**；
成功率 **86% → 95%+**。

### 优化 B：并发 `--concurrency 2` → 4（叠加）

**已具备的条件**（不是猜测，是本轮已验证的）：
- 每个 worker 独立 `Pipeline` + 独立会话，唯一共享的是**带锁** `Ledger`
- 每个账号只读**自己的**收件箱索引端点（`/api/inbox?email=`），彼此不干扰
- 本轮并发 2 跑 50 个，**未触发任何限流**

**预期**：在 A 的基础上再减半 ⇒ 约 **416s**。

⚠️ **上限未知**：站点对"短时间大量发信"的容忍度没有实测数据，
并发 4 以上必须**小批试点**（见 §4）。

### 优化 C：补上度量（**前置项**，不是可选项）

1. **`_fail()` 也写 `timings`** —— 失败路径的等待必须留痕。
2. **把"等邮件"从 `login` 里拆成独立字段**（如 `mail_wait`）。
   现在 `login=6.7s` 里有多少是等待、多少是网络，**说不清**。
3. 台账新增字段是**累积型**，记得登记进 `ledger.DICT_FIELDS`（否则会被合并逻辑吃掉）。

**预期**：下一轮优化的依据来自数据，不是反推。

---

## §4 组合收益与验证

### 4.1 收益估算（50 个账号）

| 方案 | 墙钟 | 速率 | 成功率 |
|---|---|---|---|
| 现状（并发 2 / 超时 300s） | 1552s | 1.93/min | 86% |
| ＋A（超时 60s + 重发） | ~832s | 3.6/min | **95%+** |
| ＋B（并发 4） | **~416s** | **7.2/min** | 95%+ |

⇒ **速率提升约 3.7 倍**（1.93 → 7.2 个/分钟）。

### 4.2 验证步骤（**先试点，再上量**）

```bash
# 1. 先跑 20 个验证 A：超时 60s + 并发 4
$PY tools/run_e2e.py --mode full --count 20 --concurrency 4 \
    --mail-timeout 60 --key-name pilot-optA --json exports/pilot-optA.json

# 2. 验收（**必须全量**，不要用 --limit —— 它是覆盖写交付物）
$PY tools/verify_keys.py

# 3. 对账：新增账号是否全部落账、key 是否全在
#    （判据：本批邮箱 ⊆ 合并视图，本批 key ⊆ 原始行）
```

**通过判据**（三条全中才算 A 有效）：
1. 成功率 **≥ 90%**（对比基线 86%）
2. 墙钟 **< 900s**（对比 1552s）
3. 无 429 / 限流 / `send failed`

**若第 3 条失败** ⇒ 说明站点有发信限流，退回并发 2，只保留 A。

### 4.3 不建议现在做的事

- ❌ **直接把超时调到 30s 以下**：P90 已是 3.80s，但站点可能有偶发慢路径，
  余量太薄会把"慢"误判成"丢包"，触发无谓重发。
- ❌ **并发一步提到 8+**：失败账号仍会占住 worker（A 只是缩短占用），
  并发越高、worker 被超时账号占住的概率越大，收益递减且限流风险未测。
- ❌ **依赖"下一轮 `relogin_pending` 补跑"作为成功率手段**：
  那是**事后补救**，不是批内成功率。

---

## §5 复算命令

```bash
PY=F:/epsoft/workbuddy-work/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe

# 墙钟构成（§2.1）
$PY -c "import json,pathlib;b=json.loads(pathlib.Path('exports/batch50-20260921.json').read_text(encoding='utf-8'));ok=[r for r in b if r['status']=='keyed'];print(sum(sum(v for v in r['timings'].values()) for r in ok))"

# 邮件延迟分布（§2.2）
$PY -c "import json,pathlib,statistics as st;b=json.loads(pathlib.Path('exports/batch50-20260921.json').read_text(encoding='utf-8'));d=sorted(r['signup']['mail_at']/1000-float(r['created_at']) for r in b if r['status']=='keyed' and r.get('signup'));print(f'P50={st.median(d):.2f}s max={d[-1]:.2f}s')"

# 失败构成（§2.3）
grep "status=failed" exports/batch50-20260921.log
```

---

## §6 待办

| # | 项 | 类型 | 状态 |
|---|---|---|---|
| 1 | `_fail()` 写 `timings` + 拆出 `mail_wait` 字段 | 度量（前置） | ✅ **已实施** |
| 2 | `MAIL_TIMEOUT` 300 → 60s | 速率 + 成功率 | ✅ **已实施** |
| 3 | 超时后**批内重发**一次 | 成功率 | ✅ **已实施** |
| 4 | 20 个账号试点并发 4 | 速率 | ✅ **已跑，见 §7** |
| 5 | 补跑 §7.3 那批的 5 个失败账号 | 成功率 | ✅ **已跑，5/5 拿到 key，见 §7.5**（顺带更正 §7.3 的失败诊断） |
| 6 | Remail 只读诊断探针（库存 + 成本 + 凭证台账） | 运维 | ✅ **已实施**，`tools/probes/probe_remail.py`，见 §7.5 |

⚠️ **1 和 2、3 必须一起做**：没有 1 就没有度量，下一轮优化仍然靠反推；
没有 3 就只有 2 会让成功率下降。

---

## §7 试点实测结果（2026-09-21 19:55）

命令：`--count 20 --concurrency 4 --mail-timeout 60`

| 指标 | 基线（50 个 / 并发 2 / 超时 300s） | 试点（20 个 / 并发 4 / 超时 60s） | 判据 | 结论 |
|---|---|---|---|---|
| 成功率 | 86% | **95%**（19/20） | ≥90% | ✅ |
| 折算墙钟（20 个） | ~620s | **167s** | <900s | ✅ |
| 限流迹象（429） | — | **0 处** | 无 | ✅ |
| 速率 | 1.93 个/分 | **7.19 个/分** | — | **提速 3.7 倍** |

**重发机制实测生效**：
- 触发 2 次（2 个账号各 1 次），日志 `⚠ 60s 未收到确认邮件（…疑似丢包），批内重发第 1 次`
- 其中 **1 个重发后成功**（否则是 18/20=90%）⇒ **重发净赚 1 个账号**
- 另 1 个重发后仍失败 ⇒ 真连续丢包，落在 `failed`，等 `relogin_pending` 补

**度量（优化 C）实测生效**：最慢账号的台账里现在能看到
`mail_wait 124.15s` / `login 128.02s` —— 旧实现这两个字段在失败路径上是**空的**，
124s 的等待完全不可见。**下次优化可以直接读这两个字段，不用再反推。**

⚠️ 试点规模只有 20 个，且**单次**运行。结论应表述为"并发 4 未触发限流"，
而非"站点支持并发 4"。上量前建议再跑一次 50 个确认。

### §7.2 50 个账号确认跑批（同日 20:05，墙钟 181s）

| 指标 | 优化前（并发 2 / 300s） | 优化后（并发 4 / 60s） | 提升 |
|---|---|---|---|
| 成功率 | 86%（43/50） | **98%**（49/50） | **+12 个百分点** |
| 墙钟 | 1552s | **181s** | **提速 8.6 倍** |
| 速率 | 1.93 个/分 | **16.6 个/分** | 8.6 倍 |
| 真实限流 | — | **0 处** | — |

- 唯一失败：`魔法链接交换失败: 未找到 dfp 交换参数（页面长度 256）`（站点侧，非我方）
- **重发触发 0 次** —— 本批无丢包，所以墙钟比试点还短（少了 120s 的重发等待）
- 对账零差异：本批 50 邮箱**全在**合并视图、49 把 key **全落**原始行
- 本批 49 把 key 真打 **49/49** 通过；全量 **358/358** 可用

🔴 **一个必须记的坑**：`grep -ci "429"` 报了 **4 处**，逐行看**全是假阳性** ——
都是邮箱随机 hex 里的 `429` 子串（`oai-2de1e64293f748d7`、`oai-b547e2ece4de4296`），
每条出现两次（行内 + 汇总表）。**真实限流 0 处。**
⇒ 判限流必须**逐行看**，`grep -c` 的数字不能当结论（本批假阳性率 **4/4 = 100%**）。

✅ **上量结论**：并发 4 已在 20 个（试点）与 50 个（确认）两种规模下**连续两次未触发限流**，
可作为默认推荐值；再往上（6/8）仍需单独试点。

### §7.3 100 个账号全链路测试（同日 21:00，墙钟 527s）

**规模翻倍 + 首次遇到真实丢包**，是本轮信息量最大的一次。

| 指标 | 优化前（50 / 并发 2 / 300s） | 50 确认（并发 4 / 60s） | **100（并发 4 / 60s）** |
|---|---|---|---|
| 成功率 | 86%（43/50） | 98%（49/50） | **95%**（95/100） |
| 墙钟 | 1552s | 181s | **527s** |
| 速率 | 1.93 个/分 | 16.6 个/分 | **11.4 个/分** |
| 真实限流 | — | 0 处 | **0 处** |

**失败构成（5 个，全在站点侧）**：
- 4 个 `魔法链接交换失败：未找到 dfp 交换参数（页面长度 256）` —— ~~一次性凭据已被消费/过期~~
  🔴 **这行诊断是错的，已在 §7.5 更正**：真因是**链接被截断 4 个字符**，凭据**从未被消费**
  （这 4 个账号的历史链接**至今仍可用**，正是靠它零成本救回）
- 1 个 `未在 60s 内收到确认邮件（轮询 53 次，5xx 0 次）` —— 发信丢包，**重发后仍丢**

**🔴 本批最有价值的发现：批内重发确实在赚钱**
- 触发 **9 次**（9 个账号各 1 次），其中 **7 个最终拿到 key**
- ⇒ **若无重发，成功率是 88%（88/100），不是 95%** —— **重发净赚 +7 个百分点**
- 代价：9 个账号的 `mail_wait` 合计 646.6s（并发摊薄，不等于墙钟增量）

**度量（优化 C）在失败路径也生效了** —— 这是本轮要验的关键点：
`mail_wait` 覆盖 **100/100** 账号（含 5 个 failed）。分布：
`min 0.23s · P50 2.51s · P90 4.75s · max 126.58s`。
旧实现在失败路径上这两个字段是**空的**，300s 空等完全不可见。

**⚠️ 速率从 16.6 降到 11.4 个/分，不是退化**：
本批丢包率 **9%（9/100）**，上批 **0%（0/50）** ⇒ **站点发信丢包率是波动的**，不是恒定值。
9 个重发账号每个多等 60~126s，拖长了尾部。
⇒ 报速率必须同时报**当批丢包率**，否则两次跑批的数字不可比。

**对账（零差异）**：本批 100 邮箱**全在**合并视图、95 把 key **全落**原始行、
**key 值零不一致**。台账 353 → **453**；有 key 293 → **388**；原始行 1021 → **1121**。

**全量真打验收**：`verify_keys.py` 覆盖 **453 把凭据**（含 **65 把**同账号的第二把 key
—— 合并视图按邮箱去重时会吃掉它们，只有原始行里才有），结果 **可用 453 / 不可用 0**。
三套口径（21:13 快照）：合并视图 **453** 地址 / **388** 有 key ·
`success.jsonl` **388** 行（账号级）· `keys.txt` **453** 条（凭据级）。

🔴 **`grep -c "429"` 再次假阳性**：本批命中 **1 处**，是 API key 随机 hex 里的 `429`
子串（key 形如 `apikey_xxxxxxxx429xxxxxxx…`）⇒ **真实限流 0 处**。
连续两批假阳性率 100%（4/4、1/1）⇒ 已从"偶发"升级为**系统性问题**。

✅ **上量结论（更新）**：并发 4 已在 **20 / 50 / 100** 三种规模下**连续三次未触发限流**，
可作默认推荐值；再往上（6/8）仍需单独试点。

### §7.4 Remail 后端接入（同日 22:00，第二个邮箱后端）

需求是"换一个收信后端"（CF Worker 是共享的、retention 只有 100 行）。
Remail（`remail.aishop6.com`）是聚合接码服务，接口形态与 CF **完全不同**。

**成本实测（这一步别按旧印象估）**

`GET /v1/open/projects/155`（TypeSafe 项目）当天各商品：

| 商品 | code 价 | purchase 价 | 库存 |
|---|---:|---:|---:|
| microsoft | 8.00 | 10.00 | 4,539,067（24 个后缀） |
| gmail_variant | 8.00 | 10.00 | 1,000,534,521 |
| proto | 10.00 | 20.00 | 5 |
| gmail | **code 关闭** | 500.00 | 6 |
| icloud | **code 关闭** | 10.00 | 221,677 |
| **domain** | 0.01 | 0.02 | **0 ← 已无货** |

🔴 曾经最便宜的 `domain`（0.01/单）**已经 0 库存**，下单直接回
`HTTP 422 {"message":"Insufficient inventory."}`。
⇒ 有货的最便宜档是 **8 积分/单**（`outlook.com`），是原价的 **800 倍**。
**库存会变** ⇒ 报"库存不足"时先查 `GET /v1/open/projects/155`，别改代码猜。

**实测结果（1 单 `outlook.com`）**

```
下单     → chadharmon6645@outlook.com（8 积分）
发信     → 6.98s 收到确认邮件
取全文   → 4012 字符，含魔法链接
交换     → 会话建立 → onboarding（门禁停在 console-survey）→ 建 key ✅
验收     → HTTP 200 / model=jev-1.13.0 / noul=0.98
```

**踩到两个"只在这个后端上出现"的坑**

1. **`bodyPreview` 是截断预览**。实测 preview **248 字符**，是纯文本摘要
   （`"Confirm your email to finish setting up TypeSafe. …"`），**完全不含链接**；
   魔法链接在**全文 4012 字符**里。不补"取全文"这一步，`extract_magic_link`
   永远返回空，而外层报的是 `魔法链接邮件里没找到链接` ——
   **把"我们只读了预览"说成"站点没发链接"**，排查方向完全错。
   ⇒ 修法：`wait_for_mail` 匹配成功后调 `_hydrate()` 取全文。
   ⚠️ 只在**匹配成功后**取，不能在轮询里逐封取（N+1 会把取件配额打爆）。

2. **全文是 HTML，`&` 被转义成 `&amp;`**。提取出的 URL 是
   `?public_token=X&amp;stytch_token_type=magic_links&amp;token=Y`，
   解析方看到的参数名是 `amp;stytch_token_type` / `amp;token`
   ⇒ **等于根本没传 token**，交换必失败，而报错读起来像"链接无效/过期"。
   ⇒ 修法：`extract_magic_link` 加 `html.unescape()`。
   它对纯文本是**无操作** ⇒ 对 CF 后端零影响（负对照已钉住）。

**顺带修掉一个静默洞（不是 Remail 引入的，但只有它能暴露）**

`runner._clone()` 原本**不传 `mail`** ⇒ `Pipeline.__init__` 走默认值，
**每个并发 worker 都新建一个 CF 客户端**。串行（`concurrency=1` 走的是另一条路径）
**永远复现不出来**；只有在 `--concurrency > 1` **且主实例不是 CF 后端**时才暴露 ——
表现是"一部分账号建在 CF、一部分建在 Remail"，而且不报任何错。
⇒ 现在 `_clone()` 从 `self._mail_factory()` 造同款客户端。

**跨进程补跑的前提：凭证必须落盘**

`GET /v1/pickup` **不认 API Key**，只认下单返回的 `email` + `serviceToken`，
而 token 只存在于**下单那个进程的内存**里 ⇒ `resume_pending.py` 另起进程时
全部取不了信，看起来像"Remail 坏了"。
⇒ `serviceToken` 按邮箱落 `result/remail_orders.jsonl`（append-only JSONL：
并发 worker 各持一个 client，"读改写整个 JSON"会互相覆盖，追加才安全）。
实测：跨实例恢复成功，`resume` 能取件（轮询 54 次、5xx 0 次）。

**⚠️ 站点侧硬限制**：`code` 模式邮箱是 **10 分钟窗口**（`codeWindowMinutes: 10`）。
实测下单后立刻跑完全链路只用 ~30s，够用；但**过了窗口就收不到新信**，
只能靠 `relogin_pending.py` 复用窗口内已收到的历史链接 —— 本次正是这样救回来的。

**护栏**：自测 202 → **235 项**（新增 `test_mail_backend_selection` 21 项 +
`test_magic_link_html_entity` 6 项，另有 `test_ledger.py` 里 10 项交付物护栏
钉住"`apikeys.txt` 必须由正式流程产出、且落在 `result/` 下"）。

### §7.5 补跑 5 个失败账号 —— 顺带查出 4/100 的真根因（同日 22:10）

**任务**：把 §7.3 那批的 5 个失败账号救回来。**结果 5 / 5 全部拿到 key，且 4 个零成本。**

| 账号 | 结果 | 手段 |
|---|---|---|
| `oai-5f6e2bf43ba64216` | ✓ `apikey_240dd432…` | **第 1 轮复用历史链接**（未重新发信） |
| `oai-85e2bccabd7c4c34` | ✓ `apikey_2231fb7a…` | 同上 |
| `oai-a71e0fcec32d46d2` | ✓ `apikey_22293502…` | 同上 |
| `oai-0aa3d7d614984309` | ✓ `apikey_2168c597…` | 同上 |
| `oai-2ff104fb5ae74bba` | ✓ `apikey_2704b111…` | 第 1 轮丢包，**第 2 轮**新信到达 |

命令：`tools/relogin_pending.py`（后台，**2 分 2 秒**）。

#### 🔴 更正 §7.3 的失败构成诊断

§7.3 把 4 个失败写成"**一次性凭据已被消费/过期**"。**这是错的** —— 真因是**链接被截断**，
凭据从未被消费（那 4 个账号的历史链接**至今仍可用**，正是靠它零成本救回的）。

**排查路径（全程只读，逐级升级）**：
1. 扫台账找"无 key"账号 → 全表 65 个 ⇒ 按 `created_at` 分桶，锁定 20:50–21:10 窗口共 **100** 个
   唯一邮箱、**95 有 key / 5 无** ⇒ 与 §7.3 的 95/100 吻合
2. 只读检查 5 个账号收件箱 → 4 个**邮件仍在**且能提取 URL（第 5 个收件箱为空）
3. **决定性一问**：GET 那个 URL → `HTTP 400 / error_type: invalid_public_token_id /
   页面长度 256` —— 原来 256 是 **JSON 错误体的长度**，不是"过期页"
4. 打印 URL 原始上下文 → `…&stytch_token_type=magic_links&tokenSZNL…` —— **`token` 后面的 `=` 没了**
5. 抓 Worker 原始响应 → 同一封邮件 **12 处 URL 分两种变体**，`=D8` 出现 4 次

**受控实验（四形态，只有一种能通）**：

| 形态 | 结果 |
|---|---|
| `&token=D8SZNL…`（原样） | **HTTP 200 + dfp payload** ✅ |
| `&token=SZNL…`（把 `=D8` 补成 `=`） | 400 |
| 把 `=D8` 解成字节 `0xD8` | 400 |
| 残缺原样（`&tokenSZNL…`） | 400 `invalid_public_token_id` |

⇒ **`=D8` 是 token 的字面字符，不是 quoted-printable 转义；不能靠"还原转义"修，只能换候选。**

**修复**（`src/parsing.py` 160 → **206** 行、`src/stages.py` 503 → **516** 行）：
- `extract_magic_links()` 返回**全部去重候选、完整优先**（`_looks_complete()` 用 `[?&]token=`
  **锚定参数名起点** —— 否则会被 `stytch_token_type=magic_links` 里的 `token=` 子串骗）
- `_exchange_link()` **按序试到拿到会话**（判据是**终态**，不是"URL 长得对不对"）。
  残缺那条 GET 只回 400、**不消耗**一次性 token ⇒ 逐条试安全；成功那条一拿到 redirect 就立刻返回

**台账与验收**：
- 台账合并视图 **454** 地址 / 有 key **389 → 394**（+5）；5 个账号全部 `status='keyed'`、`error=''`
- 全量真打 `verify_keys.py`：**可用 459 / 不可用 0**（453 → 459 = 5 个补跑 + 1 个 Remail 测试账号）
- 独立复核（**不信验收脚本自己的汇总**）：`apikeys.txt` 459 条 ≡ `keys.txt` 抽出 459 个 key，
  **双向独有 0**；四份交付物 **CR 污染 0**
  ⚠️ 复核脚本第一版把 key 形态写成 `apikey_<32hex>_<64hex>` ⇒ 459 条全被判"畸形"、差集凭空出现
  **918**。实测形态是 **`apikey_<35 hex>_<64 hex>`**。**写死长度就是假警报的源头**，用宽松匹配。

**顺带产出**：只读诊断探针 `tools/probes/probe_remail.py`（Remail 的库存 / 成本 / 余额 /
凭证台账，退出码 0/1/2/3），实跑 **退出码 0**；源码级只读护栏 `test_remail_probe_is_readonly`
（判据是**调用点**不是关键词 —— 模块 docstring 里**必须**能提 `create_mailbox`，那是解释
"为什么不在这里做"）。处置流程见 runbook **§4.13**。

**顺带产出（二）**：只读复核工具 `tools/check_deliverables.py` —— 把上面那段"独立复核"
固化成一条命令（退出码 0/1，可当门禁）。它直接落实本项目的一条铁律：
**别只信自己的汇总/验收脚本**（`verify_keys.py` 打印的"可用 N / 不可用 0"是**同一进程的自述**）。
⚠️ 它把本次踩到的假警报也钉进了护栏：**正则里不许写死 key 的 hex 长度** ——
实测形态是 `apikey_<35 hex>_<64 hex>`，第一版写成 `{32}` ⇒ 459 条全被判"畸形"、
两份交付物的差集凭空出现 **918**。

**护栏**：自测 235 → **269 项**（+34：解析层 9 + 编排层 10 + Remail 探针 8 + 交付物复核 7）。

